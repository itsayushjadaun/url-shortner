<!DOCTYPE html>
<html lang="en">
<head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>url shortner</title>
      <style>
            .id_url{
                  font-family: fantasy, Cochin, Georgia, Times, 'Times New Roman', serif;
                  text-align: center;
                   padding-top: 40px;
            }
            .custom-file-upload {
    border: 1px solid #ccc ;
    display: inline-block;
    padding: 6px 12px;
    border-radius:10px;
    cursor: pointer;
}
h1{
      color: cadetblue;
}
 
button{
      color: aliceblue;
      background-color: black;
      border-radius: 20px;
      margin-top: 10px;
      margin-left: 8rem;
}

.link{
      font-family: Arial, Helvetica, sans-serif;
      color:  orange;
}

td, th {
    border: 1px solid #777;
    padding: 0.5rem;
    text-align: center;
}

table {
      margin-left: auto;
      margin-right: auto;
      text-align: center;
    border-collapse: collapse;
}
 
tbody tr:nth-child(odd) {
    background: #eee;
}
      </style>
     
</head>

<body>
      <div class="id_url">
            <h1>URL shortner</h1>
            <form  method="post" action="/url">
                  <label>
                        enter your origenal url : 
                  </label>
                 <input class="custom-file-upload" type="text" name="url" placeholder="https://example.com">
                 <br>
                 <button type="submit" >Generate</button>
            </form>
            <% if (locals.id) { %>
             <p>URL Generated : </p>
             <br>
             <p class="link">http://localhost:4000/url/<%= id %></p>
            <% } %>
          
      </div>
      <div class="id_table">
            <% if (locals.urls) { %>
             <table>
                  <thead>
                        <th>S.No.</th>
                        <th>Short ID</th>
                        <th>Redirect ID</th>
                        <th>Click</th>
                  </thead>
                  <tbody>
                      <% urls.forEach((url,index) => { %>
                        <tr>
                              <td><%= index +1 %></td>
                              <td><%= url.shortId %></td>
                              <td><%= url.redirectURL %></td>
                              <td><%= url.visitHistory.length %></td>
                        </tr>

                      <% }) %>
                  </tbody>
             </table>
            <% } %>
      </div>
</body>
</html>



